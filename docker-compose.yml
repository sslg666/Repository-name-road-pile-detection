version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./static/uploads:/app/static/uploads
      - ./detection_results.db:/app/detection_results.db
    environment:
      - FLASK_DEBUG=false
      - PORT=5000
    restart: unless-stopped
