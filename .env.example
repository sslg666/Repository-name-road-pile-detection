# 环境变量配置示例
# 复制此文件为 .env 并修改配置

# Flask配置
FLASK_DEBUG=false
PORT=5000

# 上传文件夹路径（可选，默认为 static/uploads）
# UPLOAD_FOLDER=/path/to/uploads

# 数据库路径（可选，默认为 detection_results.db）
# DATABASE_PATH=/path/to/database.db
